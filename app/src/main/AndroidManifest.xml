<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.travel.bofy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BOFY">
        <activity android:name=".KylActivity"></activity>
        <activity android:name=".YlyyActivity" />
        <activity android:name=".PlatonActivity" />
        <activity android:name=".TotoActivity" />
        <activity android:name=".VotActivity" />
        <activity android:name=".RegisActivity" />
        <activity android:name=".RegistrActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>